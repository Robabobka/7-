%% 1. Создание прямоугольной матрицы A x B
A = 3;
B = 4;
M = randi([1, 10], A, B);  % случайные целые числа от 1 до 10
disp('Прямоугольная матрица M:');
disp(M);

%% 2. Обращение к элементам по индексам
element = M(2, 3);
disp(['Элемент M(2,3) = ', num2str(element)]);

%% 3. Поэлементные арифметические операции
M2 = randi([1, 10], A, B);  % вторая матрица того же размера
disp('Вторая матрица M2:');
disp(M2);

disp('Поэлементное сложение:');
disp(M + M2);

disp('Поэлементное умножение:');
disp(M .* M2);

disp('Поэлементное деление:');
disp(M ./ M2);


disp(['Определитель S: ', num2str(detS)]);

% Обратная матрица (если определитель не нуль)
if detS ~= 0
    invS = inv(S);
    disp('Обратная матрица S^(-1):');
    disp(invS);
else
    disp('Матрица S вырожденная, обратной не существует.');
end

% Собственные значения
eigVals = eig(S);
disp('Собственные значения S:');
disp(eigVals);
